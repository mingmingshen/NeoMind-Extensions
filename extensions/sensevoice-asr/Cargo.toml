[package]
name = "sensevoice-asr"
version = "2.7.9"
edition = "2021"
authors = ["NeoMind Team"]
license = "Apache-2.0"
description = "SenseVoice-Small ASR extension (Mandarin/English/Japanese/Korean/Cantonese) via sherpa-onnx ONNX CPU backend"

[lib]
name = "neomind_extension_sensevoice_asr"
crate-type = ["cdylib", "rlib"]

[dependencies]
neomind-extension-sdk = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
semver = "1"
base64 = "0.22"
parking_lot = "0.12"
tracing = "0.1"

# Native (non-wasm) deps
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
chrono = "0.4"
tokio = { version = "1", features = ["rt", "sync", "rt-multi-thread"] }
ureq = { version = "3", features = ["json"] }
